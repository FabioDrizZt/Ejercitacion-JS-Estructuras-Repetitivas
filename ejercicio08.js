// Ejercicio 8
// Obtener el resto de la división entera a%b de dos números enteros positivos mediante restas.

function restoPorRestas(a, b) {
    // Tu código aquí
    // Debe retornar el resto de dividir a entre b
    // Si b es 0, debe lanzar un error
}

module.exports = {
    restoPorRestas
};
