// Ejercicio 5
// Imprimir los números pares desde N (siendo N un número par que se lee) en forma descendente
// hasta 2.

function generarParesDescendentes(n) {
    // Tu código aquí
    // Debe retornar un array con los números pares desde n hasta 2 en orden descendente
    // Si n no es par, debe lanzar un error
}

module.exports = {
    generarParesDescendentes
};
